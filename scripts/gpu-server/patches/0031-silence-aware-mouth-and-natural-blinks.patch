--- a/avatars/audio_features/base_asr.py
+++ b/avatars/audio_features/base_asr.py
@@ -24,6 +24,8 @@
 import torch.multiprocessing as mp
 
 from avatars.base_avatar import BaseAvatar,AudioFrameData
+from gpu_services.livetalking_gateway.avatar_runtime import audio_frame_type
+
 
 
 class BaseASR:
@@ -51,7 +53,12 @@
         self.queue.queue.clear()
 
     def put_audio_frame(self,audio_chunk:NDArray[np.float32],datainfo:dict): #16khz 20ms pcm
-        self.queue.put(AudioFrameData(data=audio_chunk,type=0,userdata=datainfo))
+        # Upstream labels every uploaded/TTS chunk as speech, including
+        # trailing zero PCM.  The visual pipeline then continues to infer an
+        # active mouth after the audio has audibly ended. Preserve the audio
+        # samples, but route genuinely quiet chunks through the neutral frame.
+        frame_type = audio_frame_type(audio_chunk)
+        self.queue.put(AudioFrameData(data=audio_chunk,type=frame_type,userdata=datainfo))
 
     #return frame:audio pcm; type: 0-normal speak, 1-silence; eventpoint:custom event sync with audio
     def get_audio_frame(self)->AudioFrameData:        
--- a/avatars/base_avatar.py
+++ b/avatars/base_avatar.py
@@ -47,6 +47,10 @@
 
 from utils.logger import logger
 from utils.image import read_imgs,mirror_index
+from gpu_services.livetalking_gateway.avatar_runtime import (
+    NaturalBlinkScheduler,
+    apply_synthetic_blink,
+)
 
 # class State(Enum):
 #     INIT=0
@@ -66,6 +70,10 @@
         self.sample_rate = 16000
         self.chunk = self.sample_rate // (opt.fps*2) # 320 samples per chunk (20ms)
         self.sessionid = self.opt.sessionid
+        self._echo_blink_scheduler = NaturalBlinkScheduler(
+            f"{self.sessionid}:{getattr(opt, 'avatar_id', '')}",
+            fps=max(1, int(getattr(opt, 'fps', 25))),
+        )
 
         self.speaking = False
         self.recording = False
@@ -450,6 +458,20 @@
                 else:
                     combine_frame = current_frame
 
+            # Apply a subtle, non-periodic eye-only blink after both talking
+            # and idle composition. This guarantees life-like blinking even
+            # when the source scan contains no naturally closed-eye frame,
+            # without replacing or warping the mouth generated by MuseTalk.
+            blink_amount = self._echo_blink_scheduler.next_amount()
+            try:
+                face_box = self.coord_list_cycle[idx]
+            except (AttributeError, IndexError, TypeError):
+                face_box = None
+            if blink_amount > 0.0 and face_box is not None:
+                combine_frame = apply_synthetic_blink(
+                    combine_frame, face_box, blink_amount
+                )
+
             cv2.putText(combine_frame, "LiveTalking", (10, 20), cv2.FONT_HERSHEY_SIMPLEX, 0.3, (128,128,128), 1)
             
             # 使用统一输出接口推送视频帧
